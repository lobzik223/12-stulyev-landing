export type CityScene = {
  id: number;
  slug: string;
  title: string;     // Старгород, Васюки...
  summary: string;   // краткий момент сюжета
  quote?: string;
  image: string;     // /images/journey/*.jpg
};
